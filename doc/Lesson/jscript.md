const "x" : permet de declarer une variable "x" qui ne change pas

let "x" : permet de declarer une variable "x" dont la valeur peut changer au cours du programme

var "x" : permet de declarer une variable "x" qui peut changer, mais a une portée plus large que "let"

x : nom du variable

document' .z ' : objet global en JS qui represente la page HTML entière chargée dans le navigateur

z = .querySelector(' y ') : méthode JS pour sélectionner un élément dans la page

'y' : selecteur CSS passé en paramètre à queryselector()

function : definit une fonction

create' x ' : créer ' x ' 

z = .classlist : une propriété qui contient toutes les classes CSS associées à un élément HTML

Z = .add(' y ') : méthode pour ajouter une classe à l'élément

